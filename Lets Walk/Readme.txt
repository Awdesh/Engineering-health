Instructions-:

Javascript API to calculate time in mins required by a person to walk to lead active lifestyle.
Call API like this-:
Sample Client call
   function getRemainingTimeNeedToWalk() {
   	var timeWalkedInMins = 30;
	var time = FitBit.timeRequiredToStayActive(timeWalkedInMins);
}

You'll see time in mins need to maintain active lifestyle.